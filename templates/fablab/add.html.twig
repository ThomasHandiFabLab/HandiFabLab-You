{% extends 'base.html.twig' %}

{% block title %}Ajouter un fablab{% endblock %}

{% block body %}
    <div class="container my-4">
        <div class="my-2">
            {{ form_row( form.name) }}
        </div>
        <div class="my-2">
            {{ form_row( form.address) }}
        </div>
        <div class="my-2">
            {{ form_row( form.city) }}
        </div>
        <div class="my-2">
            {{ form_row( form.cp )}}
        </div>
        <div class="my-2">
            {{ form_row( form.nbProject )}}
        </div>
        <div class="my-2">
            {{ form_row( form.phonenumber )}}
        </div>
        <div class="my-2">
            {{ form_row( form.email )}}
        </div>
        <button class="btn btn-primary">Ajouter</button>
    </div>

{% endblock %}

